
  # flashCard

  This is a code bundle for flashCard. The original project is available at https://www.figma.com/design/z8WIZaoMJsnYUBj4cOfjiF/flashCard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  